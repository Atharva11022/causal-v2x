# Environment
venv/
__pycache__/
*.pyc
.DS_Store

# Data (large / regenerable — do not commit)
data/*.csv
data/*.zip
data/*.txt
data/*.pdf
data/*.htm
data/v2x_seq_spd_example*
data/v2x_seq_tfd_example*
data/*_extracted/

# Model weights & intermediate arrays (regenerable via scripts)
outputs/*.pt
outputs/*.npy

# Keep small plots and summary CSVs as reproducible evidence.
!outputs/*.png
!outputs/*.csv

# Local legacy report awaiting rewrite for the current TFD implementation.
docs/report.md
docs/Internship_Report.docx
docs/Internship_Report.pdf
docs/presentation.pptx

# Kaggle credentials — NEVER commit this
kaggle.json

# VSCode
.vscode/*
!.vscode/settings.json
